name: CI
on: [ push ]
jobs:
  building:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@master

      - name: build
        shell: bash
        run: |
          ls 
          git submodule update --init --recursive
          cmake -B build
          cd build
          make
          cd ..
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: test
        shell: bash
        run: |
          ls
          cd build
          ./tests